<h1>Admin::Admin#index</h1>
<p>Find me in app/views/admin/admin/index.html.erb</p>
<h1>Mon Profil</h1>

<div class="container">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Prenom</th>
          <th scope="col">Nom</th>
          <th scope="col">Email</th>
          <th scoe="col">Show</th>
          <th scoe="col">Delete</th>
        

        </tr>
      </thead>
      <tbody>
<% @user.each do |user| %>    
        <tr>
          <td><%= user.first_name %></td>
          <td><%= user.last_name %></td>
          <td><%= user.email %></td>
          <td><%= link_to "Delete", admin_user_path(user.id), :confirm => "Are you sure?", :method => :delete %></td>
        </tr>
        <tr>
        <%end%>
      </tbody>
    </table>

    <table class="table">
      <thead>
        <tr>
          <th scope="col">Title</th>
          <th scope="col">result</th>
          <th scope="col">Delete</th>
        

        </tr>
      </thead>
      <tbody>
    
<% @foot_print.each do |foot| %>
        <tr>
          <td> <%= foot.product.title %> </td>
          <td><%= foot.result %></td>
          <td><%=link_to "Delete", admin_foot_print_path(foot), :confirm => "Are you sure?", :method => :delete  %></td>
        </tr>
<% end %>
        <tr>
      </tbody>
    </table>
<div id="menu">
<%= link_to "Modifier le profil", edit_user_registration_path(current_user.id), class:"nav-link"%>
</div>






