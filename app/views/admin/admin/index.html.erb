<br>
<br>
<br>
<br>
<h1 class="text-center">Admin Index</h2>
<div class="container">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Prenom</th>
          <th scope="col">Nom</th>
          <th scope="col">Email</th>
          <th scoe="col">Show</th>
          <th scoe="col">Delete</th>
        

        </tr>
      </thead>
      <tbody>
    <hr>
      <h4>Last <%= @user.count %> user</h4>   
    <hr>
<% @user.each do |user| %> 
        <tr>
          <td><%= user.first_name %></td>
          <td><%= user.last_name %></td>
          <td><%= user.email %></td>
          <td><%= link_to "more" , admin_user_path(user.id) %></td>

          <td><%= link_to "Delete", admin_user_path(user.id),data: { :confirm => "Are you sure?"}, :method => :delete %></td>
        </tr>
        <tr>
        <%end%>
      </tbody>
    </table>

    <table class="table">
      <thead>
        <tr>
          <th scope="col">Title</th>
          <th scope="col">result</th>
          <th scope="col">Delete</th>
        

        </tr>
      </thead>
      <tbody>
    

    <hr>
      <h4>Last <%= @foot_print.count %> user</h4>   
    <hr>
<% @foot_print.each do |foot| %>
        <tr>
          <td> <%= foot.product.title %> </td>
          <td><%= foot.result %></td>
          <td><%=link_to "Delete", admin_foot_print_path(foot), data: {:confirm => "Are you sure?"}, :method => :delete  %></td>
        </tr>
<% end %>
        <tr>
      </tbody>
    </table>
<div id="menu">
<%= link_to "Modifier le profil", edit_user_registration_path(current_user.id), class:"nav-link"%>
</div>






